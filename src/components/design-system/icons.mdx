import { Meta, IconGallery, IconItem } from "@storybook/blocks";

import * as Icons from "@tabler/icons-react";

console.log(Icons)

<Meta title="Design System/Icons" />

<IconGallery>
{Object.entries(Icons).filter(([name]) => name !== "iconList").map(([name, Icon]) => (<IconItem name={name}>
<Icon />
</IconItem>))}

</IconGallery>
